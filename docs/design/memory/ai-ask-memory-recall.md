---
status: draft
last_updated: 2026-09-19
---

# ai_ask 的记忆调用

> **本文回答一个问题**：ai_ask（脚本里的提问动作）在什么情况下要查会谈记忆——每种情况怎么认出来、要查时发什么过去、查到怎么用。
>
> **怎么读**：§1 四个问题，§2 怎么判，§3 怎么认出钩子、检索词怎么写（本文重心），§4 代价与边界，§5 意识层那条异步线。案例集与落地细则在附录，读机制不必翻。

**关联**：[记忆框架](memory-framework.md) · [变量-记忆桥接](variable-memory-bridge.md)（变量值怎么刷新） · [意识系统设计](../consciousness/consciousness-system.md) §2.6/§2.7（意识层调取） · [ADR 007](../decisions/007-variable-document-boundary.md)（变量权威在文档） · [ADR 006](../decisions/006-retrieval-layer-scope.md)（检索层范围）

---

## 1. 四个问题

主线 LLM 的 prompt 里只有「最近聊天」的有限回合——**本次会谈早些的内容、跨会谈的内容都不在**。凡是 AI 要用到"聊过、但最近对话里没有"的东西，就是记忆该介入的时候。ai_ask 上有四个这样的时候：

**一、听不懂用户在说什么。** 用户说「上次说的那个事，我回去又想了想」「李光最近又联系我了」——指的是什么，最近对话里没有。不查记忆，AI 只能反问"李光是谁？"

**二、被反问，答不上依据。** 用户说「那你觉得我该怎么办？」——AI 这一轮从追问者变成回答者。真要给建议，那句话得有既往方案、用户试过什么、效果如何做依据。不查，只能给空泛的通用建议，像第一次见面的机器人。

**三、要收的信息，用户以前讲过，又问一遍。** 脚本规划本话题要了解「小时候的主要抚养者」，不代表用户之前没提过。收集前先回顾一遍，AI 开口就能带上"你之前提过…"，而不是让用户把讲过的话再讲一遍。

**四、不是"回复缺东西"，是"觉察缺证据"。** 上面三问的触发者都是用户的话——AI 要开口，发现手边没有。还有一类，触发者是引擎自己的觉察：后台觉出一个信号，要判断这信号真不真、是不是趋势，也要证据。

- 「我其实挺喜欢和人打交道的」——和既往那句「我讨厌社交」顶上了：是他真变了，还是我听岔了？
- 「我最近好像没那么容易生气了」——真好转，还是这一周顺？
- 「有时候真想一了百了」——既往提过没有？有没有一起定过安全计划？

这一类不走主线，各意识在自己的后台通道里各查各的（§5）；触发通道为什么不同，见 §1.1。

**前三个问题看着不同，其实是同一条：AI 这一轮要说的话，指着一个它看不见的东西。** 本文把它叫**钩子**——§2 的判定、§3 的检索表达都绕着它转。第四个问题不问钩子，问"证据够不够"，走的是另一条通道。

### 1.1 全景：记忆在四个点介入

```
进入话题 ──▶ [三] 收集前回顾：本话题要收的变量里，值还空着的 → 代码直接查（每话题一次）
                │
用户说话 ──▶ [一/二] 判定字段：none 直接回 / defer 先回、下轮补上 / block 先查再回
                │
                ▼
         记忆片段并入 {{memory_context}} ──▶ 主线 LLM 生成回复
                │
后台 ──────▶ [意识层] 矛盾 / 情绪轨迹 / 阻抗 / 危机 各自 recall —— 不占用户等待
```

执行者只有两类，这就是"快通道 / 慢通道"的全部含义——**分的不是记忆的种类，是谁在等**：

- **主线**（快通道）：用户等着回复，所以必须快——在用户可接受的等待预算内跑完的那条链。触发者是用户的话（问题一~三），判断并入主线生成（§2），不额外调用 LLM；查记忆本身是一次本地 HTTP。
- **意识层**（慢通道）：后台跑，用户不等——可以慢，可以用 LLM 做深度分析（§5）。触发者是引擎自己的觉察（问题四）。

## 2. 怎么判

判断者是**主线 AI 自己**。它在生成这一轮回复的**同一次生成**里，输出的**开头**先给判定字段，其余字段照常跟在后面——模板那份输出本来就是一份 JSON，判定四字段排在最前。引擎读到它、把它摘掉，据此决定这一轮怎么走；判定字段**不给用户看**。

判定只回答一件事：**只靠这些已知（最近对话 + 已收集变量），够不够支撑这一轮的回复。**

四个字段**按这个顺序写**（都是输出的最前面，其余字段照常跟在后面）：

| 字段       | 装什么                                                                                                                            |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `reason`   | 一句话说明为什么这么判——给 `none` / `defer` 写"钩子是什么"，给 `block` 写"缺了什么"。**给日志与实证看**：用户看不到，下游也不用它 |
| `recall`   | 这一轮怎么走——三种取值，见下表                                                                                                    |
| `entities` | 这一轮原话里的专名（人名、地名、机构）：有才给                                                                                    |
| `query`    | 检索词（§3.1 / §3.2 两张表）：要查时给，不查时空串                                                                                |

> **顺序是设计的一部分，不是排版。** 基于大模型是串行生成，前面输出的token影响后面的特性，这里特安排先输出“reason”。这通常能提升“recall”判断的准确性，相当于是要它**先把"缺什么"想清楚**，再落判定和检索词——若排在最后，就成了写完之后的事后合理化，对检索词一点影响都没有；`entities` 排在 `query` 前，是**先把人名交代出去**，`query` 才好只管"事"（§3.3）。同样的 token 数，换个位置而已。（这条待实测：附 C.3 ⑤）

**三种取值**：

| `recall` | 这一轮怎么走                                                       | 什么时候用                               |
| -------- | ------------------------------------------------------------------ | ---------------------------------------- |
| `none`   | 不查，直接回复                                                     | 靠已知够用——大多数轮                     |
| `defer`  | 先回复（回复里不含任何对未知信息的预设），同时查一次，**下轮**带上 | 有钩子，但不写它这一轮也答得完整         |
| `block`  | 这次生成到此收笔 → 查一次 → 记忆注入后**重新生成**再回复           | 有钩子，不写它就答不出、答空，或只能反问 |

**判据是两句，按顺序问**：

1. **这一轮有没有钩子？** 钩子 = 指向「你说过、但我看不见的东西」的话头（§1 定义；§3 给识别它的路标）。**没有 → `none`**——寒暄、表达情绪、回应上一句，大多如此。
2. **有钩子就不许猜它——把这一轮想说的那句话先写出来，看它带不带那个东西**：
   - **带**（话里必须交代它是谁 / 是什么 / 发生过什么 / 该怎么办，不写就答不出、答空或只能反问）→ `block`。
   - **不带**（不写它也完整：共情、接着问、确认一句）→ `defer`；并自检：这句话里不能出现任何对它的预设——不猜它是谁、不编他说了什么。
   - **两档之间拿不准 → `block`**：多查的代价是慢一轮，猜错的代价是不实的回复，两个方向不对等。

判据落在"这一轮想说的那句话写不写得出来"上——是模型做得到的一次试写，不是让它估自己"接不接得住"。**判据里没有"查不查得着"**：模型读不到记忆库，估不出查了到底有没有；碰不中只能反问，那是 `defer` 存在的理由，不是每天用的判据。

**为什么并进生成，而不是单起一道判定**：判定要用的上下文（原话、最近对话、变量）主线本来就要预填，并进生成，边际成本只有那几十个输出 token；单起一道要**多预填一份 prompt**，且**每轮都要等**它。咨询场景大多数轮不需要查，按轮平摊这笔账赢在常见情形上。代价是要查的轮多一次重新生成——重开就得把主线 prompt 再预填一遍（有前缀缓存则基本打平）。

**调用侧四条**：

1. **头部缓冲**：判定字段读出来之前，输出不发给用户（界面只有"正在思考"态）；到上限仍读不出，走第 4 条。
2. **判 `block` 就收笔**：提示词要求"判 `block` 时写完这四个字段就停"（附 B）——这次生成自然结束，引擎不必去学"掐断流"（各家中止语义不一，还要处理竞态）。模型不听、继续往下写也照旧丢弃，第 1 条保证用户看不到。`reason` 排在最前还有个附带好处：收笔时它已经把"缺什么"写完了。
3. **只重新生成一次**：第二次生成时提示词注明"记忆已注入，直接作答，判定字段给 `none`"；**每个用户输入最多查一次**（硬限，防打转）。
4. **判不出来就按 `block` 兜底**：解析从宽（空格、大小写、多余字段都算合法）；仍读不出判定字段，就弃掉这次生成，按附 C.3 的固定拼法查一次、重新生成。不为此再起一次 LLM 调用。

> 支持 assistant 预填充的模型，把开头预填成 `{"reason":"` 能显著提高格式守约率（开头是 `reason`，预填充串随之改）。

**这套判法的两个风险**，都已记在案：

- **模型可能"装作知道"**：判定与回复由同一个模型在同一次生成里完成——它不认识「李光」，却可能顺着话头顺畅地编下去。对冲是提示词里那条硬规则（专名在最近对话里没出现过 → 必须 `block`，附 B），外加一套**字面检查**每轮跑一遍（词表 + 分词，毫秒级、不调 LLM）**只记录不干预**——它说"要查"而判定给 `none` 的轮次，正是这个风险的样本（附 C.4）。
- **判 `block` 等于"这一轮先不说话"**：提示词若写成"你不许说话"，模型就可能为了开口而倾向 `none`。附 B 因此把它写成流程的正常一步：你先判，引擎把记忆给你，你再完整作答。判断与正文表达要抢同一份注意力，判得准不准、正文有没有被拖累，都得实测（附 C.3）。

## 3. 识别特征与检索表达

三处要查的地方共用一套账：**先在用户话里找路标**（§3.1）→ **再看这一轮要向记忆要哪一面**（§3.2）→ **按组合规则拼出检索词**（§3.3）。§3.4 起是三个时机各自的判定线。

### 3.1 路标：用户话里给了什么线索

| 路标     | 例                         | 进 query 的方式                                         |
| -------- | -------------------------- | ------------------------------------------------------- |
| 专名     | 李光、阿黄                 | 原样进 query，**同时**进 `entities`                     |
| 指代词   | 那件事、老样子、你上次说的 | 原话本身就是钩子，整句进 query                          |
| 人称代词 | 他、她、他们               | 原话进 query——信息量低，靠变量名补范围                  |
| 时间词   | 上个月、上次会谈、最近     | 原样进 query——它同时是给检索侧的新鲜度信号（§3.2 末行） |
| 事件词   | 被同事出卖、出车祸         | 原样进 query——**比变量名具体，优先于变量名**            |
| 无       | 嗯、唉、我也不知道该怎么说 | 没有路标 → 没有钩子 → `none`                            |

> 专名和代词要分开看：**专名是硬的**——它在最近对话里出现过没有，一眼可判，没出现过就是硬规则必查；**代词是软的**——「他」指向谁，得看最近对话里对不对得上人。

### 3.2 缺什么：这一轮要向记忆要哪一面

| 缺什么                       | 什么时候                       | query 问的面                                             |
| ---------------------------- | ------------------------------ | -------------------------------------------------------- |
| 这个人是谁、与来访者什么关系 | 专名 / 代词                    | 「{名字}——他是谁、与来访者什么关系、两人之间发生过什么」 |
| 这件事是什么                 | 泛指指代                       | 原话 + 变量名（圈范围）                                  |
| 这件事的前情与结果           | 用户讲了件**可能已经聊过**的事 | 事件词 + 「以前聊过没有、聊到哪、后来怎样」              |
| 既往方案与他试过的           | 反问、这一轮要引依据           | 「此前聊过的应对方案、他试过哪些、效果如何」             |
| 这件事最新的一次             | 同一主题聊过多次               | 主题 / 变量名 + 「最近一次」                             |

> **同一件事聊过多次时，要的是最近那一次**——「那件事」有时间性。每轮 recall 都带 `queryTimestamp`（相对时间表达的解析基准、recency 评分的时间锚，§4.2），检索表达再把时间意图写明——两道一起用。能不能稳定挑出"同主题最后一次"，靠实证（附 C.3 ⑥；验收案例 26）。

### 3.3 组合规则

一轮常常不止一个钩子——「我该不该跟李光断了？」= 反问 + 专名。拼检索词时：

- **人给 `entities`，事给 `query`**：专名进 `entities`（它管"这个人是谁、与来访者什么关系"），`query` 只管"事"；
- **时间词叠加**进 query；
- **变量名只在它确实圈定范围时才拼**，最多 2 个——**指代跨到别的话题时不拼**，否则检索整批被拉去当前话题的旧料（案例 13）；
- **指代类整句带上原话**：原话就是钩子本身，它指出这一轮具体在说哪件事、哪个人；
- **硬规则**：人名、地名、机构名在最近对话里没出现过 → 必查（`block`）；
- **不编造**：`query` 里不出现用户没说过的具体人名、事件；`entities` 只写这一轮原话里的专名——从记忆里想起的名字是要查的东西，不是查询条件；
- **具体优先**：专名、事件、时间线索、用户的原话词，能带的都带上。30~80 字是"别塞噪声"，不是"越短越好"。

> **原话与变量名各干什么**：原话指出这一轮在说哪件事；变量名圈范围（脚本里写死的，永远可用）。只有原话会泛泛召回，只有变量名则不知道这一轮问的是哪一面——**要不要都用，是判定字段自己的判断**。

### 3.4 指代：用户说「那件事」「李光」

**三种判定的线画在哪**：

| 判定    | 线画在哪                                                             | 例子                                                                           |
| ------- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| `none`  | 所指就在最近对话里——AI 自己看得见                                    | 「那个方案」上一句刚提过；「李光」最近对话里出现过；「他」能对上人             |
| `defer` | 有钩子，但这一轮想说的那句话不写它也完整，且回复里不含任何对它的预设 | 「还是老样子」→ 先接住："嗯，还是老样子。这阵子过得挺累的吧？"                 |
| `block` | 不写它就答不出、答空，或只能反问「你指的是？」                       | 「上次那个方案」最近对话里没有；**「李光」没出现过（硬规则）**；「他」对不上人 |

> 指代够不够用，要看它指的是哪一段：最近 2-3 轮内的、已收进变量的、本话题更早没进变量的、上次会谈有专名的、上次会谈无专名的——分别靠主线自己 / 变量值 / 召回 / 人物卡 / 反问解决。**这张表说的是"靠什么解决"，不是"查不查"**；查不查由上面三档定。对照见附 A.2。

### 3.5 反问：用户说「那你觉得我该怎么办？」

**识别特征**：用户把问题抛回来——「你觉得呢」「我该怎么办」「你怎么看」「你说呢」「我该不该」。这一轮 AI 从追问者变成回答者。

但**"反问"只是时机，不是判据**。这一轮到底走哪个方向——给建议、先澄清、只共情、还是把"你想让我替你做决定"本身当材料谈——由咨询框架定；记忆只在"**这一轮定下来的那句话，缺不缺最近对话里没有的事实**"上说话。

**三种判定的线画在哪**：

| 判定    | 线画在哪                                                                          | 例子                                                                                 |
| ------- | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| `none`  | 这一轮定下来的那句话**不引既往事实**                                              | 先澄清："你要的是我拿个主意，还是先一起把这事看明白？" · 只共情 · 把反问本身当材料谈 |
| `defer` | 先用不依赖既往事实的话接住（共情 / 反过来问），依据下轮带上                       | "你问我该怎么办——这事压了你挺久了吧？"                                               |
| `block` | 这一轮**真要给建议**、要引"上次我们聊过…"——那句话缺既往方案、他试过什么、效果如何 | "上次我们聊到…你说试了三天有点用，要不这次…"                                         |

**这一处最容易判反**：把"反问"当成"必查"，一上来就给 `block`——白花一次检索 + 一次重生成，还挤掉一次本该先澄清的机会（案例 12 与 12-a 配对看）。

另有一种情况：这一轮的话里带专名（「我该不该跟李光断了？」）→ 查，但**不是因为它是反问**，是因为那个名字在最近对话里没出现过——归 §3.3 的硬规则。

**检索词按 §3.2 的"既往方案与他试过的"那一行写**——**不抄原话**，写"这一轮需要什么面"：`来访者问「该怎么办」——此前聊过的应对方案、他试过哪些、效果如何`。查的是既往方案与他试过的，不是"我该怎么办"这五个字。带专名时按 §3.3 拆开：`{"query":"李光——他是什么人、与来访者是什么关系、两人之间发生过什么事","entities":["李光"]}`。

### 3.6 收集前回顾：要收的信息，他讲过没有？

**这一处不用判定**——要收哪些变量、值空不空，代码都算得出来。

**识别特征**：进入话题时（该话题第一个动作执行前），静态解析本话题内全部 ai_ask 的 `output` 变量，取**当前值为空**的（文档装载没读到、本会话也还没收到）。这一处**没有用户原话**。

**三档在这一处退化成两行**：

| 情况       | 做什么                                                      |
| ---------- | ----------------------------------------------------------- |
| 变量全非空 | 不查——文档装载或本会话已经收到，记忆不掺和                  |
| 有空值     | **代码直接查**：拼检索词 → recall → 注入本话题首问的 prompt |

**为什么这里没有三档**：判定字段存在的理由是"信息不对称"——有些判断只有模型做得来（所指在最近对话里还是不在）。这一处不存在不对称：变量名和话题名都在代码手里，代码不比模型少知道什么。让它判一次，等于白花一次生成。

**检索表达**：`话题名 —— 关于{空值变量名}`，例如 `童年抚养者——关于主要抚养者、抚养方式`。这里没有原话可挖，模型能贡献的只有措辞，不值得为它多一次生成。

**结果怎么用**：注入「本话题已知信息」段，本话题内每个 ai_ask 都带着它；AI 开口自然衔接——"你之前提过小时候主要是奶奶带大的，那时候的日子，你愿意多说说吗？"变量值仍由用户回复提取（§4.1 边界）。

**为什么在话题入口**：本话题要收的变量清单静态可解析**且确定**——话题队列会被意识层调整（插入 / 重排 / 深度切换），但进话题时它都在；一次查询摊到本话题多个 ai_ask 上，每话题最多 1 次检索。

> 进话题那一轮，用户可能也在说话（上一话题的收尾回复）——这一处的检索与判定字段可能触发的那一次各发各的，目的不同；"每个用户输入最多查一次"管的是判定那一次。

> §3.5 的 `defer` 那一行、§3.6 的「无三档」结论，是按 §2 的判据推出来的写法，尚无实测——这两处随实现观测，判错就改。

## 4. 代价与边界

### 4.1 边界：记忆不写变量值

**记忆的回顾结果只作为上下文给 AI 看，不写进变量的值。**变量的权威是[信息点文档](../decisions/007-variable-document-boundary.md)（ADR 007 决策 2）。

**为什么空值变量需要回顾**：跨会话值的权威是信息点文档，脚本工程师在脚本里声明变量与信息点的映射。但**没被映射的变量，文档里根本没有这个信息点**——而"用户以前讲过没有"不取决于脚本有没有计划收集它。所以：装载之后**值仍为空**的待收集变量，先回顾一次（§3.6）。

**为什么不写值**，三条指向同一件事：值一旦写入并驱动退出决策（"这个变量有了，不用再问"），本次会谈就少收集了本该收集的信息，而人能改的是文档、这一项恰恰没有信息点；记忆值可能过时、不精确或表达形式不同——让它带着写一句确认式提问"你之前提过…，是这样吗？"是稳妥的，固化成当前值等于把不确定性藏进流程控制；写进去就产生消费依赖，下游 condition / 退出决策按精确标量用，会逼出 bridge 那套新鲜度与合并策略的复杂度（ADR 007 决策 6 刚把它砍掉）。

**与变量刷新的分工**（[variable-memory-bridge](variable-memory-bridge.md) §4.1 piggyback）：两者都是"提前准备"，区别在产出——一条只把记忆给 AI 看（本文），一条刷新变量的值（bridge）。

| 维度     | 本文：只给上下文              | bridge：刷新变量值                                             |
| -------- | ----------------------------- | -------------------------------------------------------------- |
| 触发时机 | 进入话题时（收集前）          | Action N 执行时（前瞻 N+1 引用的变量）                         |
| 目标集   | 本话题待收变量中**值为空**者  | N+1 引用的变量 ∩ `autoRefresh ≠ false`——**与"是否待收集"无关** |
| 产出     | 上下文（注入 prompt，不写值） | 变量值（写 store，`source: 'llm_piggyback'`）                  |

**一条待补的闸门**（落在 [bridge 修订故事（Epic D）](../../scrum/backlog.md)）：本话题待收集且当前为空的变量，不应进 piggy 的目标集——现行 bridge 的写入门槛只有"非空"、没有"原本有值"，而临床判断类默认 `autoRefresh: true`（恰是待收集变量最常见的类）。漏洞是 ADR 007 决策 6 的残留。**另有一条写值的路是合法的**：已映射变量在装载时的 recall 兜底（ADR 007 决策 4，走显式 fallback 链，发生在会话启动）；本节管的是**无映射**的空值待收集变量。

> 升级路径（待实证）：若实证发现"AI 明明看到了回顾内容，仍从零追问"，再考虑把回顾结果写进变量 store——先验证不加的情况。

### 4.2 代价：多快、几次、留多久

- **判定不额外调用 LLM**：它只是主线那次生成输出开头的几十个 token，不需要查的轮（大多数）只多这一点时间。整轮预算尚未定义，本文只约束下面这几条。
- **要查的轮多一次重新生成**：判 `block` 时模型收笔，记忆到手后重新生成一次；预填成本取决于 provider 的前缀缓存。
- **一次查记忆 ≤200ms**（本地 HTTP 到 Hindsight，纯检索不调 LLM）；超时或 Hindsight 不可用 → 返回空、静默降级，不阻塞主流程。
- **每个用户输入最多查一次**：一次判定给一个检索词，`defer` 与 `block` 都只查这一次，重新生成不再判、不再查。
- **联想只保留最近 5 轮**（滚动淘汰）：5 轮前的联想大概率已失效，同时防止 `memoryContext` 无界膨胀。`defer` 查到的记忆下轮带上，天然落在里面；意识层的洞察不受此限。
- **人物卡不随轮丢**（附 C.1）：**人**的属性不该按轮过期——李光是谁、跟来访者什么关系，第 6 轮和第 5 轮一样要紧。所以实体态取出后按名字存成卡，会话内留着；**渲染**则只在"这一轮提到这个人"时出现（判定字段写了这个专名，或代码在最近对话里扫到这个卡里已有的名字）。卡按次淘汰（末次提及最旧的先走，上限 10 张），**不额外发检索**——卡来自既有的那次查。
- **实体态每轮都取**（不按轮切），上限 200 token；本轮的「相关人物」与本轮记忆片段是两笔独立预算。取回来是为喂卡，不是为渲染（附 C.6）。
- **归纳优先于原始片段**（`prefer_observations`）：谈到一件事时，要的是"这件事的概要"，不是某一次聊天的完整片段——Hindsight 把 experience 归纳成 observation 时，开启这个开关会让被归纳过的原始片段让位给归纳本身（腾出的位置补后面的结果，不会变少）。原始片段不丢：observation 上挂着 `sourceFactIds`，要证据时按 id 回查（附 C.2）。
- 三路扩展（附 C.5，默认关闭）若启用并超预算，降级顺序：先砍关联事件 → 再砍类似事件 → 只留原始查询。
- **投机检索不做**：检索本来是一次本地调用，可以在主线开始生成的同时用原话先发一次——判 `none` 就白花一次（用户无感），判 `block` 时那 200ms 就不在关键路径上。收益有限（判定字段本身也要时间），且它把"每轮都花一次检索"当默认——等实测出"要查的轮里检索等待占比明显"再谈。

### 4.3 犯错时往哪边倒

**先分清三种错——只有一种能放过。**

| 错                       | 代价                                      | 自愈吗                                         |
| ------------------------ | ----------------------------------------- | ---------------------------------------------- |
| **漏查**（该查没查）     | AI 追问一轮，"李光是谁？"                 | **自愈**——用户当场就说了，下一轮信息自己进来了 |
| **误查**（查了不该查）   | 白花一次检索（判 `block` 时多一次重生成） | 不用治——判定字段不给用户看，用户无感           |
| **装作知道**（编出来的） | 用户**看不出来**，纠正不了                | **不自愈**——这才是要挡的那个                   |

- **漏了就漏了**：不设检测器、不设补救流程，就当这位咨询师这轮没记起来——用户会补一句，下一轮就有了。**不为它加第二次 LLM 调用，也不把主线判定质量的账记到意识层头上**（两处都没人认领）。
- **编造另外算**：它不是"记忆不好"，是硬规则要挡的——专名在最近对话里没出现过 → 必须查（§3.3）。这条对冲的是"顺着话头编下去"，不是靠事后检测。
- **宁可多查**：误查的代价已经是最小的（一次本地检索，用户无感）；漏查和编造都要用户买单。判定拿不准时倾向查——这条纪律写在判定段的提示词里（附 B）。
- **查不到就反问，不猜**：记忆里没有，AI 自然问"你指的是？"——真人咨询师也这么干。回顾与召回都**只增不减**：召回为空时，行为与没有这个机制时完全一致。

## 5. 意识层调记忆

ai_ask 只负责主线上的三处（§1 问题一~三）。**觉察判断类的调取**（§1 问题四）不归它——那些是意识层的活，各意识在自己的后台通道里 recall，不占主线延迟预算（机制见[意识系统设计 §2.7](../consciousness/consciousness-system.md)）。**触发者是引擎自己的觉察，不是用户的话**——这是它和主线三处的根本区别，也是它为什么可以慢。

| 调取方 | 通道                           | 触发                               | 查询                              |
| ------ | ------------------------------ | ---------------------------------- | --------------------------------- |
| 主线   | 同步：判定随生成 + 检索 ≤200ms | 指代 / 反问两处（§3.4 / §3.5）     | 判定字段写出的检索词              |
| 主线   | 同步：进话题时一次，代码直接发 | 本话题有待收空值变量（§3.6）       | 话题名 + 空值变量名               |
| 各意识 | 异步（后台）                   | 各意识 `recall.enabled` + 触发阈值 | 各意识 `recall.query_hint` + 原话 |

各意识各自要什么证据，举例（不穷举，机制在各意识自己的定义里）：

| 意识                   | 要判断的                       | 要查的证据                               |
| ---------------------- | ------------------------------ | ---------------------------------------- |
| `contradiction_detect` | 这轮说的与既往顶上了，谁是真的 | 既往那几句的原话与当时语境               |
| `emotion_trajectory`   | 是真好转，还是这一阵顺         | 跨会谈的情绪状态记录                     |
| `resistance_monitor`   | 这次的阻抗是新的还是老样子     | 既往的阻抗信号与当时的干预               |
| 危机趋势               | 这个信号要不要升级             | 既往危机记录与一起定过的安全计划         |
| `strategy_adaptation`  | 新提的事和既往议题有没有关系   | 既往议题与已试过的策略（话题衔接，待定） |

**触发**：某个意识的 `insightForSlowThinking`（string，见[意识系统 §2.3.3](../consciousness/consciousness-system.md)）非空时，触发深度 recall + LLM 综合。主线 LLM 不感知、不负责触发——主线回应来访者，意识层觉察信号并决定"什么值得深挖"。

**ai_ask 侧的契约**（硬约束）：

1. **fire-and-forget**：异步运行，不阻塞主线（5-30s）。
2. **产出 DeepInsight 存入 `session.metadata.deepInsights`**（类型见[意识系统 §2.6](../consciousness/consciousness-system.md)）。
3. **注入前经时效性门控**：渲染「深度洞察」段时只取 `relevanceCheck != "过期"` 的条目（附 C.1）。
4. **只有建议权**：洞察注入 prompt 后由主线 LLM 自己判断是否采纳；不直接修改脚本流程（不跳过话题、不插入动作）。

---

## 附 A：案例集

> **场景常量**：第 3 次会话；ai_ask 正在收集 `{核心信念, 睡眠质量}`；`u` = userId；变量名串 = `关于核心信念、睡眠质量`。
> 「判定」= **期望的**判定字段输出（措辞由模型定）。两处不适用：案例 1-3 归装配侧、不发判定（§3.6），案例 21-25 走意识层（§5）。**本案例集是验收案例的单一出处**。
> **四处最容易判错的**：案例 4（先看「那个事」所指在不在最近对话里——在就不查）、案例 6（专名没在本轮最近对话里出现过 → 查，并写进 `entities`）、案例 13（指代跨话题时可以不带变量名）、案例 20（人称代词：最近对话里能对上人就不查）。其余按 §2 的判据走。
>
> **怎么当测试集跑**：每个用例三件输入——① 最近对话里有什么、没有什么 ② 变量当前值 ③ 本轮原话。「判定」列就是**期望输出**，比对的是 `recall` 取值与检索词的关键成分（专名进没进 `entities`、变量名拼没拼、时间意图带没带），不比措辞——措辞由模型定（附 C.3 ②）。两处一条原话对应两种状态、要拆成两个用例各跑一次：案例 4 与案例 20。反问轮的 12 与 12-a 是同一输入的两个分支——分的是"这一轮咨询框架定下来的那句话"，夹具里钉住走哪一支。三处长文（9、11、18）的正文属测试数据，放夹具不放本文：9 要三条连发、指代落在第三条，11 要指代落在中段，18 要通篇无指代。

**按类索引**（§3 的两张表要**逐类测**——哪一类漏得多，看这张表；附 C.3 ②）：

| §3.1 路标 | 案例                    | §3.2 缺什么          | 案例                |
| --------- | ----------------------- | -------------------- | ------------------- |
| 专名      | 6、12-b                 | 这个人是谁、什么关系 | 6、12-b、20         |
| 指代词    | 4、7、8、10、11、13、26 | 这件事是什么         | 4、7、9、10、11、13 |
| 人称代词  | 20                      | 这件事的前情与结果   | 5、8                |
| 时间词    | 10、11                  | 既往方案与他试过的   | 12、12-a            |
| 事件词    | 5                       | 这件事最新的一次     | 26                  |
| 无        | 14-19                   | —                    | —                   |

### A.1 收集前回顾（案例 1-3）

> 场景：进入话题「童年抚养者」，两条 ai_ask 分别收 `{主要抚养者}`、`{抚养方式}`。

| 案例 | 情况          | 发生什么                                                                                                         |
| ---- | ------------- | ---------------------------------------------------------------------------------------------------------------- |
| 1    | 空值 + 记忆有 | 均为空 → 代码拼 `童年抚养者——关于主要抚养者、抚养方式` → 命中"第 2 次会谈：小时候主要是奶奶带大的，父母常年在外" |
| 2    | 非空 → 跳过   | 文档装载后变量全非空 → 不发检索——权威在文档，记忆不掺和                                                          |
| 3    | 空值 + 记忆无 | 回顾空手 → 按原样从零提问；回顾只增不减                                                                          |

### A.2 指代轮（案例 4-11、13、26）

| 案例 | 本轮原话                                               | 判定与检索词                                                                                                                                                                                                               |
| ---- | ------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 4    | 上次说的那个事，我回去又想了想                         | 所指在最近对话里 → `none`；不在 → `block` + 原话 + 变量名（原话零信息，变量名是唯一指路的）                                                                                                                                |
| 5    | 上次你说睡前别看手机，我试了三天，但一躺下就想工作的事 | `block` + 原话全量 + 变量名；「工作的事」是这一轮的新钩子，牵引前几轮没命中的区域                                                                                                                                          |
| 6    | 李光最近又联系我了                                     | `block` + `query:"李光——他是什么人、与来访者是什么关系、两人之间发生过什么事"` + `entities:["李光"]`——专名够硬，不带变量名                                                                                                 |
| 7    | 就是睡不着，还是老样子                                 | `block` + 原话 + 变量名——「老样子」是空钩子，只能靠变量名指路（大概率与前几轮重复，无害且刷新近期记忆）                                                                                                                    |
| 8    | 你上次问我的时候我说'还好'，其实那次之后更糟了         | `block` + 原话 + 变量名——「你上次问我的时候」是强标记；原话查询命中率最高的一类                                                                                                                                            |
| 9    | 连发三条，指代落在最后一条                             | `block` + **先拼接**成一条 + 变量名——不拼接会漏掉落在后几条里的指代                                                                                                                                                        |
| 10   | 上次会谈聊到的那个方案，我回去试了，好像有点用         | `block` + 原话 + 变量名——跨会谈指代，最近对话里没有                                                                                                                                                                        |
| 11   | 500 字长文，指代落在中段                               | 判定字段读得到全文，带不带中段的专名由它定；**固定拼法**下中段会被截断丢失（附 C.5 分段检索是改进方向）                                                                                                                    |
| 13   | 我妈上次说的那句话，我一直记着                         | `block` + **只写原话、不带变量名**——指代跨到别的话题，变量名会把检索拉去当前话题的旧料                                                                                                                                     |
| 26   | 那件事我还是过不去，一想起来就烦                       | `block` + 原话 + 变量名——**同一主题前后聊过两次**（第 1 次只提一句、第 2 次细讲经过），要的是**最近那一次**：检索词写明时间意图（主题 +「最近一次」），`queryTimestamp` 给时间锚。与案例 10 的区别：那里同一件事只聊过一次 |

**指代够不够用，看它指的是哪一段**（这张表说的是"靠什么解决"，不是"查不查"）：

| 「他 / 那件事 / 李光」指的是                          | 主线自己懂吗                         | 靠什么解决                               |
| ----------------------------------------------------- | ------------------------------------ | ---------------------------------------- |
| 最近 2-3 轮内的事                                     | 懂                                   | 靠主线自己                               |
| 本话题更早、但已收进变量                              | 大致懂——变量值是有损摘要，细节可能缺 | 靠变量值（细节缺时按"碰不中"处理）       |
| 本话题更早的原文、没进变量                            | 不懂（已经不在最近对话里）           | 靠召回——碰得中就有，碰不中就没有         |
| 上次会谈的**人**、**有专名**（案例 6 的李光）         | 不懂                                 | 靠人物卡（实体态，附 C.6）               |
| 上次会谈的**事**、**有专名**（"那个方案"有名字）      | 不懂                                 | 靠召回——专名进 `query`，按语料碰         |
| 上次会谈的**人 / 事**、**无专名**（"上次那个老同学"） | 不懂                                 | 只能靠语义碰——碰不中就反问               |
| 三样都没有                                            | 不懂                                 | 靠反问「你指的是？」——真人咨询师也这么干 |

### A.3 反问轮（案例 12、12-a、12-b）

| 案例 | 这一轮定下来的那句话                     | 判定                                                                                                 |
| ---- | ---------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| 12   | 真要给建议、要引"上次我们聊过…"          | `block` + `query:"来访者问「该怎么办」——此前聊过的应对方案、他试过哪些、效果如何"`（不抄原话）       |
| 12-a | 先澄清（"你要的是主意，还是先看明白？"） | `none`——给 `block` 就是白花一次检索 + 一次重生成，还挤掉一次本该先澄清的机会                         |
| 12-b | 「你说我该不该跟李光断了？」             | `block`——专名交给 `entities`，`query` 只管"事"；**要查不是因为它是反问**，是名字没在最近对话里出现过 |

### A.4 判 `none` 的（案例 14-19）

| 案例                                                             | 为什么判 `none`                                                       |
| ---------------------------------------------------------------- | --------------------------------------------------------------------- |
| 14「嗯」                                                         | 无钩子——AI 刚问完，过渡词无需查                                       |
| 15「你们这个 AI 问来问去有什么用，根本帮不了我」                 | 元对话攻击，无钩子——阻抗处理归意识层（案例 23）                       |
| 16「唉，我也不知道该怎么说，就是很烦，烦得不行，感觉哪儿都不对」 | 情绪宣泄无钩子——本轮原话就在最近对话里，AI 懂她在说什么               |
| 17「哦不对，我说错了，不是天天失眠，是就这两周特别严重」         | 自我更正无钩子——更正内容照常进主线上下文                              |
| 18 500 字失眠夜长文（无指代）                                    | 长文本身不是要查的理由——没有钩子同样给 `none`；内容留给 retain 侧沉淀 |
| 19「嗯，但是我还是睡不好」                                       | 转折句无钩子；内容就在最近对话里                                      |

### A.5 边界（案例 20）

**案例 20｜「他还是那样」**——判定字段识别人称代词，看最近对话里能不能对上人：**对得上 → `none`；对不上 → `block`** + 原话 + 变量名（查询对"他指谁"帮不上，变量名划出范围）。

### A.6 意识层的调取（案例 21-25）

> 机制在[意识系统设计 §2.7](../consciousness/consciousness-system.md)，此处只做指针。

| 案例 | 本轮原话                                                 | 所属意识                                      |
| ---- | -------------------------------------------------------- | --------------------------------------------- |
| 21   | 「我其实挺喜欢和人打交道的」（与既往「我讨厌社交」矛盾） | contradiction_detect                          |
| 22   | 「我最近好像没那么容易生气了」                           | emotion_trajectory                            |
| 23   | 「你们这个 AI 问来问去有什么用」                         | resistance_monitor                            |
| 24   | 「有时候真想一了百了」                                   | 危机信号评估（紧急路径）                      |
| 25   | 「我最近开始跑步了」                                     | strategy_adaptation（话题衔接提案扩展，待定） |

> 案例 24 在主线侧无钩子，快通道跳过；危机趋势的判断证据由意识层在紧急路径上 recall 取得——主线 / 意识层分工的典型样本。

---

## 附 B：判定段的提示词与输出契约

> 判定段**并入主线提示词**（不是一次独立调用）：主线 AI 每次回复，在输出开头先给判定字段，其余字段照常——与模板原有的那份 JSON 同一份输出、同一次解析。
> **住所**：Default 层模板 `config/prompt-defaults/ai_ask_v1.md` 的最末，与它的输出格式块合写；Custom 层可**整段覆盖**（沿用 Custom > Default 两层解析）。
> **接口由代码写死**：四个字段名与三个取值；模板能改的只是措辞——改了字段名，解析即失败、按 `block` 兜底（§2 第 4 条）。装载时模板校验盯这一条。
> **判定段只管两处**：§3.4 指代与 §3.5 反问。§3.6 收集前回顾不发判定（代码直接查）。
> **判定与回复用同一个模型**，这正是「判定并入生成」的含义。判不出来时按 `block` 兜底（§2 第 4 条）；若"并入生成"整体不达标（格式老守不住、延迟超标），退路是**把这段判定拆成一次独立的 LLM 调用**，不是回头用规则路径。

### B.1 判定段（写进模板的哪一段、长什么样）

位置：模板的**最末**，紧挨它的输出格式块——格式约束离生成越近，守约率越高；判定四字段则写在**输出的开头**（模板那份 JSON 的头四个字段）。

```
## 开口之前：先写判定字段

在你输出的 JSON 最前面加上下面四个字段，**按这个顺序写**，其余字段照常跟在后面（严格 JSON，不加代码块围栏）：

{"reason":"…","recall":"…","entities":[…],"query":"…", …其余字段照常…}

它回答一件事：只靠上面那些已知（最近对话 + 已收集变量），够不够支撑你这一轮的回复。

reason 先写（一句话）：给 none / defer 写"钩子是什么"，给 block 写"缺了什么"。
先把"缺什么"想清楚，再落 recall 和 query。它是给日志看的，用户看不到。

recall 三选一：
{"recall":"none"}                                  ← 够用，直接回复
{"recall":"defer","query":"…","entities":[…]}      ← 有钩子，但不写它这一轮也答得完整：先接住，记忆下一轮补上
{"recall":"block","query":"…","entities":[…]}      ← 有钩子，不写它就答不出、答空，或只能反问

怎么选（按顺序问两句）：
- ① 这轮有没有指向「你说过、但我看不见的东西」的话头？没有 → none。
  大多数轮如此：寒暄、表达情绪、回应你上一句，或指代的东西就在上面的最近对话里——你看得见，不用查。
- ② 有钩子，就不许猜它——把这一轮想说的那句话先写出来，看它带不带那个东西：
  带（话里必须交代它是谁 / 是什么 / 发生过什么 / 该怎么办，不写它这一轮就答不出）→ block。
  不带（不写它也说得完整：共情、接着问、确认一句）→ defer：先接住，记忆下一轮补上；
  回复里不能出现任何对未知信息的预设——不要猜那个人是谁、不要编他说了什么。
  拿不准带不带 → block。多查只慢一轮，猜错是不实的回复。
  人名、地名、机构名在最近对话里没出现过 → block；以往会谈里的事、你只能靠记忆才知道的事，同样 → block。
  判 block 时，写完这四个字段就停笔，不要再写其余字段——引擎会把记忆给你，你随后再完整作答。
  这是流程的正常一步，不是「这轮不让你说话」。

entities 只列本轮用户原话里的专名（人名、地名、机构、作品名），普通名词不放；不要写你从记忆里想起的名字——那是要查的东西，不是查询条件。没有则 []。

query（30~80 字）——两步装出来：**先在用户话里找路标，再看这一轮要向记忆要哪一面**。

路标（用户话里有什么，就原样带上什么）：

- 专名（李光、阿黄）→ 原样进 query，同时进 entities
- 指代词（那件事、老样子、你上次说的）→ 整句原话进 query，它本身就是钩子
- 人称代词（他、她）→ 原话进 query，信息量低，靠变量名补范围
- 时间词（上个月、最近、上次会谈）→ 原样进 query
- 事件词（被同事出卖）→ 原样进 query，它比变量名具体，优先于变量名

要哪一面（这一轮缺什么，就问什么）：

- 这个人是谁、与来访者什么关系 →「{名字}——他是谁、与来访者什么关系、两人之间发生过什么」
- 这件事是什么 → 原话 + 当前话题的变量名
- 这件事的前情与结果（可能以前聊过）→ 事件词 +「以前聊过没有、聊到哪、后来怎样」
- 既往方案与他试过的（这一轮要引依据）→「此前聊过的应对方案、他试过哪些、效果如何」，不抄原话
- 这件事最新的一次（同一主题聊过多次）→ 主题 / 变量名 +「最近一次」

- 一轮可能不止一个钩子（「我该不该跟李光断了？」= 反问 + 专名）：**人给 entities、事给 query**；时间词叠加进 query。
- 不写整句问句，写"需要什么面"；具体优先——专名、事件、时间线索能带的都带上。30~80 字是"别塞噪声"，不是"越短越好"。
- 变量名只在它确实圈定范围时才拼上（如"关于核心信念、睡眠质量"），最多 2 个，不要机械罗列；**指代跨到别的话题时不要拼变量名**。
- none 时 query 为空串。除了变量名和"需要什么信息"的描述，不要出现用户没说过的具体人名、事件。

两个臂分工：专名给 entities（这个人是谁、与来访者什么关系），query 只管"事"——「你说我该不该跟李光断了？」写成
{"entities":["李光"],"query":"李光——他是什么人、与来访者是什么关系、两人之间发生过什么事"}，别把"关系"整件事都压给 query。
```

### B.2 样例（判定字段 + 其余字段连起来长什么样）

```
【1 · 专名指代 → block】
{"reason":"「李光」是专名，最近对话里没出现过，我不知道他是谁、跟来访者什么关系","recall":"block","entities":["李光"],"query":"李光——他是什么人、与来访者是什么关系、两人之间发生过什么事"}
（写完即停笔 → 引擎查记忆 → 重新生成：判定给 none，正文写进 content）
{"reason":"记忆已注入","recall":"none","content":"李光又来找你了——你之前提过他，是你前同事，一直想拉你合伙创业。他这次联系你，你挺烦的。……"}
（这一轮检索回来的「李光」实体态进人物卡，之后提到他就直接带上——附 C.1）

【2 · 指代落在最近对话里 → none】
最近对话：… AI：上次那个方案后来怎么样了？
{"reason":"「那个方案」所指就在最近对话里，我看得见","recall":"none","content":"那个方案你又想了想——是觉得哪一块不合适？"}

【3 · 反问 → block】
{"reason":"这一轮我要给建议，需要既往方案与尝试记录做依据","recall":"block","entities":[],"query":"来访者问「该怎么办」——此前聊过的应对方案、他试过哪些、效果如何"}

【4 · 情绪表达 → none】
{"reason":"纯情绪宣泄，没有指向最近对话外的东西，接着共情即可","recall":"none","content":"唉，这种"哪儿都不对"的感觉，挺熬人的。……"}

【5 · 反问，但这一轮不引既往事实 → none】
{"reason":"这一轮先澄清他要的是建议还是空间，不引任何既往事实","recall":"none","content":"你问我该怎么办——你是想让我拿个主意，还是先陪你把这事看明白？"}
```

> 样例里模板的其余字段（`response_plan` / `assessment` / `progress` / `exit` …）照常写，为省篇幅只留了 `content`。收集前回顾没有判定样例——它不发判定（§3.6），样例见案例 1。

### B.3 输出契约（写在调用侧）

| 项             | 约定                                                                                                                                                                                    |
| -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 住所           | Default 层模板的最末；Custom 层可整段覆盖。Default 这份随引擎版本走                                                                                                                     |
| 接口           | 四个字段名与三个取值由代码写死；模板改了字段名即解析失败、按 `block` 兜底                                                                                                               |
| 位置与顺序     | 输出的**开头**——判定四字段排在模板那份 JSON 的最前，其余字段跟在其后；**顺序固定为 `reason` → `recall` → `entities` → `query`**（先写因、再落果，§2），解析不依赖顺序但从宽要求守这个序 |
| 解析           | 从输出开头读四字段，**从宽**（空格、大小写、多余字段都算合法），成功 → 按 `recall` 处理                                                                                                 |
| 调用侧行为     | 头部缓冲 / 收笔 / 只重生成一次 / 按 `block` 兜底 四条，全文只在 §2 讲——那里是唯一出处                                                                                                   |
| 不管收集前回顾 | §3.6 不进判定段：代码解析空值变量（0 LLM）后直接拼检索词、直接查                                                                                                                        |

---

## 附 C：细则

### C.1 数据结构与渲染

recall 结果合并进 `session.metadata.memoryContext`，下一轮 `buildSystemVariables()` 自动带入 prompt。各段各有自己的生命周期：

```typescript
interface SessionMemoryContext {
  baseline: MemoryContext; // 会话启动时加载的基础画像，会话内不变
  topicPrefetch: Array<{
    // 收集前回顾：进入话题时写入，本话题内有效（§3.6）
    topicId: string;
    variables: string[]; // 本次纳入回顾的变量名
    context: MemoryContext;
  }>;
  fastHits: Array<{
    // 要查的轮的联想：最近 5 轮，滚动淘汰
    round: number;
    query: string;
    context: MemoryContext;
  }>;
  entityCards: {
    // 人物卡：按人存，不随轮丢（§4.2、C.6）
    [canonicalName: string]: {
      observations: Array<{ text: string; mentionedAt?: string }>; // Hindsight 的实体态原样存
      lastMentionedRound: number; // 渲染排序与淘汰用
      lastFetchedRound: number; // 这张卡是哪一轮的检索带回来的
    };
  };
  deepInsights: DeepInsight[]; // 意识层洞察：持久累积，带时效性标记
}
```

`formatMemoryContext()` 分段渲染（`base-action.ts`）：

```markdown
## 关于来访者

{baseline}

## 本话题已知信息

{topicPrefetch 中当前话题的条目}

## 本轮相关人物

{这一轮提到的人，各渲染一张卡：判定字段写了他，或代码在最近对话里扫到卡里已有的名字}

## 本轮相关记忆

{fastHits 最近 5 轮合并渲染}

## 深度洞察

{deepInsights 中 relevanceCheck="有效" 的条目，按时间倒序}
```

> **人物卡为什么单列一段**（原来是附在那一轮里的）：因为它的生命周期**不跟轮次走**。人是会谈里的常驻对象——李光是谁、跟来访者什么关系，第 6 轮和第 5 轮一样要紧，按轮淘汰等于把"人"当成"某一轮的联想"。所以卡按人存（会话内留着），渲染条件也改成"这一轮提到这个人"，而不是"这一轮查过记忆"。它和 fastHits 是两笔账：**fastHits 是"最近几轮聊过什么"，卡是"这个人是谁"**。

**淘汰规则**：baseline 不变；topicPrefetch 话题级——离开话题即失效；fastHits 滚动保留最近 5 轮（与渲染同数——留着不渲染的轮次没有机制会回头去看）；entityCards 按次淘汰——末次提及最旧的先走，上限 10 张，会话结束即消失（它是 prompt 侧缓存，权威始终在 Hindsight，清了再取一次就是）；deepInsights 持久保留，`relevanceCheck="过期"` 的不展示，超过 10 条移除最旧。

**卡的渲染触发怎么判**：判定字段的 `entities` 只管"这一轮原话里的专名"，所以「他还是那样」这种话不会写进 `entities`——但卡里可能已经有"李光"。补一条**零 LLM 的字面匹配**：拿卡里的名字去扫最近对话的文本，扫到就渲染。要找的名字是现成的，不是猜的，比判定段那套分词可靠得多（§2 的字面检查是另一回事——那个要猜专名，这个不用）。

### C.2 查哪种记忆

| 记忆类型        | 对"理解用户这轮的话"的帮助                                                                      |
| --------------- | ----------------------------------------------------------------------------------------------- |
| **experience**  | 最直接——"用户之前说过类似的经历吗？"                                                            |
| **observation** | 高——用户/咨询师沉淀过的信念与模式（"我觉得吃药没用"被 consolidate 成带证据引文的观察，ADR 005） |
| **world**       | 低——用户陈述的事实性信息（"在 XX 公司工作"），主要用于避免事实错误                              |

recall 只有这三类可取；**案例公式化（咨询师的工作假设）不在表里**——它走 Hindsight 的 mental model，是另一套 API，会话启动时读入（ADR 005 决策 2）。

**不做类型区分，而是显式传三类**：Hindsight 的 `types` 不传时默认只返回 world + experience，observation 会被静默漏掉（SDK v0.9.1 `RecallRequest.types`）。所以每轮 recall 固定带 `['world', 'experience', 'observation']`，语义匹配到的都返回、按相关度排序；LLM 自己判断哪些有用。每轮 recall 都带当前时间（`queryTimestamp`）——"上次 / 最近"这类相对时间表达靠它解析。

**概要优先于原始片段**：`preferObservations: true`（SDK v0.9.1 `IncludeOptions`）。observation 是 Hindsight 从多条 raw fact 归纳出来的，打开后**已被归纳掉的那些原始 fact 不再单独返回**，空出的位子由后面的结果补上（条数不减）。要的正是这个——「谈到一件事，给的是这件事的概要」，而不是十几条零碎的对话片段。它是**渐进的**：只压制"已经被归纳过"的 fact，归纳尚未发生的照样返回，所以不会把记忆掏空；被压掉的原文也还在，observation 带着 `sourceFactIds`，要展开细节顺着这条线索走（ADR 005 决策 1 的保结构映射）。当前端口已经有这个参数（`RecallOptions.preferObservations`），只是从来没开过（C.7）。

### C.3 固定拼法与待验证清单

**固定拼法**（只在判定字段读不出来时用）：原话 ≤200 字则 `原话 + " —— 关于" + 变量名`；超过则取「头 100 字……尾 100 字」。变量名最多 2 个（按声明顺序），原话不清洗（embedding 对语气词、错别字鲁棒）。200 字是 embedding 输入预算的安全上限——它是"装不装得下"的边界，不是"有没有用"的边界；钩子的数量与字数不成正比，固定截断对长文必损，判定字段自己写检索词则不受这条约束。

**待验证**（前三条决定这套机制值不值）：① 判定字段的格式守约率；② 案例集当测试集跑，逐条比对漏查 / 误查；③ 「大多数轮不需要查」这个假设——它成立，并进生成这笔账才赢；④ 判断与正文抢注意力对回复质量的影响；⑤ **`reason` 写在 `recall` 之前是否真的让检索词更准更稳**——同一案例集跑两种字段顺序，比 query 命中率与判定一致性（§2"先写因、再落果"是推断，不是实测）；⑥ **"同一主题最后一次"能不能稳定挑出来**——`queryTimestamp` 只提供时间锚，"哪一次是同主题的最后一次"要靠语义判断（§3.2）；⑦ 前缀缓存未命中时那一轮的实际延迟；⑧ 模型自己收笔、记忆到手后**接着同一条生成往下写**（省掉第二次预填）在自家 provider 上是否可行。

### C.4 字面检查（只记录，不参与判定）

每轮跑一遍：词表正则（那件事 / 老样子 / 你上次说的 / 他 / 我该怎么办 …）+ 分词未知词（不在通用词库、非停用词的 token 视为专名候选），毫秒级、不调 LLM。**它只记录**——"它说要查而判定给 `none`"的轮次是"装作知道"风险的样本，攒够样本再决定要不要给它否决权（§2）。

已知边界（实测）：同一个名字换句式就漏（「李光最近又联系我了」能切出「李光」，「李光是我前同事」被切成「李 / 光是 / 我前」）；纯指代词分不清所指还在不在最近对话里。**这是"字面规则"这条路的天花板，不是词表长短的问题**——所以它只做探针，不做判手。

> **让它来真判是走不通的**：要理解语义就得再请一个 LLM，那已经不是"检查"，是第二次判定——多一次调用、多一份延迟，换来的只是"错得少一点"（§4.3：漏查会自愈，不值得为它加一层）。真到"判定并入生成"顶不住的地步（格式老守不住，或延迟超标），退路是**把判定拆成一次独立的 LLM 调用**（附 B 的判定段原样搬出去），不是回头用它。

### C.5 扩展项（待实证，默认关闭）

**三路扩展**：原始查询 + 类似事件（抽象）+ 关联事件（时间窗口），见 [memory-retrieval-types.md §2.11](memory-retrieval-types.md)。抽象走规则、零 LLM；默认关闭，先验证不加的情况。

**分段检索**：长文（案例 11）在固定拼法下中段会丢；判定字段自己写检索词则不受这条截断规则约束。优先级取决于这类长文有多常见。

### C.6 人物卡的数据从哪来（实体态；已核实 SDK，待实证质量）

**Hindsight 已经在维护"这个人的当前认识"，而且默认就送回来。** 写时（retain）它把人名、关系抽成实体节点；读时 `response.entities` 里带的是实体自己的 observations——`EntityStateResponse = { entity_id, canonical_name, observations[{text, mentioned_at}] }`，SDK 对它的原话是 "current mental model of an entity"（v0.9.1 `dist/index.d.ts`）。**「李光的关键信息」的直接来源就是它**：不用我们自己总结，也不用额外调一次 LLM。取回开关在 `IncludeOptions.entities` 上，**不传就有**（`null` 才是关掉）；`chunks`、`source_facts` 才是默认关的那两个。

三层各归各：

| 层           | 谁管                   | 怎么做                                                                 |
| ------------ | ---------------------- | ---------------------------------------------------------------------- |
| **检索臂**   | Hindsight 的 bank 配置 | `enable_graph_retrieval`，不在调用侧                                   |
| **取回**     | 每次 recall            | 默认就带；`entities: {max_tokens}` 给预算（值待实测，先 200，见 §4.2） |
| **存与渲染** | 我们自己（附 C.1）     | 按 `canonical_name` upsert 成人物卡；只在"这一轮提到这个人"时渲染      |

**为什么每轮都取、不按需取**：判定漏写专名时（比如那句是「他还是那样」），实体态已经在手里了，不用回头再查一次；成本只是一次 HTTP 里多几十行 JSON，不占 prompt 预算（渲染才占，而渲染是有条件的）。

**三处待实证**：① Hindsight 按 query 怎么挑实体——精确取名字，还是语义召回一片（影响"没提到李光的那轮会不会回来一堆人"）；② observations 的质量与颗粒度——真能给到「前男友 / 他出过轨」这一层，还是只有名字与共现；③ 存量记忆有没有实体节点（写时抽取是后加的能力，老数据未必有）。三条都能用案例集验；取不到时的退路是反问，不猜（§4.3）。

> **直查单体有一条路，暂不走**：后端有 `GET /entities`（列表）与 `GET /entities/{entity_id}`（单体），但只在 SDK 生成层（`listEntities` / `getEntity`），高层 `HindsightClient` 上没暴露。暂不用——recall 已经把实体态带回来了，为它单开一条调用等于多一次 HTTP 去取手边就有的东西。若实测发现"卡被淘汰了、这个人又被提到"这种情况常见，再走这条路补一刀。

**当前实现差距**：适配器只读 `response.results`，实体段整个被丢了（C.7）。

### C.7 当前实现差距

| 现状                                             | 目标                                                                        |
| ------------------------------------------------ | --------------------------------------------------------------------------- |
| `processUserInput()` 不调 recall                 | 判定字段判"要查"时就地 recall（§2 / §3.4 / §3.5；检索词按 §3.1-§3.3 拼）    |
| 无判定                                           | 主线回复输出开头给判定（§2）：附 B 判定段 + 解析 + 头部缓冲 + 兜底          |
| recall 仅 `initializeSession()` 一次，查询硬编码 | 收集前回顾（代码直接查，§3.6）+ 要查的轮由判定字段写检索词（§3.4 / §3.5）   |
| `memoryContext` 扁平结构                         | 分层结构（C.1）                                                             |
| 无意识层触发机制                                 | 意识层触发 → fire-and-forget 深度分析 → 注入后续轮次（§5）                  |
| `formatMemoryContext()` 单一渲染                 | 分段渲染 + 轮次标注 + 时效性标记 + 「本轮相关人物」单列一段（C.1）          |
| 无 DeepInsight 概念                              | 意识层产出结构化洞察卡片                                                    |
| 适配器只读 `response.results`，实体被丢          | 取回 `response.entities` → 按名字 upsert 成人物卡（C.1 / C.6）              |
| port 的 `RecallOptions` 无实体预算               | 端口补一条实体预算（SDK 侧是 `entities: {max_tokens}`，恒定取、先 200）     |
| `recall()` 只回 `MemoryContext` 三个数组         | 返回值要能带出实体态——它不进 `MemoryContext`，进 `entityCards`（C.1 / C.6） |
| `preferObservations` 端口已有、从未开启          | 每轮 recall 打开，概要优先于原始片段（§4.2 / C.2）                          |

> `require` 收集紧迫度已移出本文——属 exit-decision 关切，schema 中无此字段（ADR 006 决策 6），见 backlog 故事。
